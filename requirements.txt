# Core dependencies
numpy>=1.21.0
torch>=2.0.0
torchvision>=0.15.0

# Hardware Control (LeRobot 대신 직접 구현)
dynamixel-sdk>=3.7.0  # Dynamixel 모터 제어
# lerobot>=2.0.0  # 참고용으로만 사용, 실제 의존성 제거

# Computer Vision
opencv-python>=4.5.0
pillow>=9.0.0

# Data Processing
pyyaml>=6.0
tqdm>=4.60.0
pyarrow>=10.0.0
pandas>=1.3.0

# ROS2 Integration
rosbags>=0.9.0
transforms3d>=0.4.1

# HuggingFace Integration (학습용)
huggingface-hub>=0.19.0

# Optional: RealSense support
# pyrealsense2>=2.50.0

# Optional: GUI support
# PyQt5>=5.15.0

# Note: ROS2 packages should be installed via apt:
# sudo apt install ros-humble-ros-base ros-humble-cv-bridge ros-humble-rosbag2
